import React from "react";
import styled from "styled-components";

interface ICategoryProps {

}

const Container = styled.div`
    width: 1440px;
    height: 100%;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
`;

export function PLP(props: ICategoryProps) {
    return (
        <Container>
            <h1>ghdfhjdf</h1>
        </Container>
    );
}